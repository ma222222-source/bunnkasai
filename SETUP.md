# 黒工文化祭 校内マップ v3 セットアップ・運用マニュアル

## いま何をすればいいか（最短の6手順）

1. **GitHub に上げる** … `index.html` `sw.js` `manifest.json` の3つをリポジトリのルートへ
   （フォルダごとではなく**ファイルを選んで**ドラッグ）。
   **`manifest_1.json` のように末尾に `_1` が付いているものは削除**し、必ず `manifest.json` の名前にする。
   名前が違うとオフライン用の仕組みが丸ごと動きません。
   `コード_2.gs` も削除（公開リポジトリにスプレッドシートIDが載ってしまうため）
2. **GAS を差し替える** … Apps Script のコードを全消しして `Code.gs` を貼り、`migrate` を1回実行
3. **パスワードを入れる** … プロジェクトの設定 → スクリプトプロパティ → `ADMIN_PASS`（英数字12文字以上）
3-2. **タイムゾーンを東京にする** … スプレッドシートの「ファイル → 設定 → タイムゾーン」と
   Apps Script の「プロジェクトの設定 → タイムゾーン」を両方 **(GMT+09:00) 東京** にする。
   米国のままだと、来場者カウンタが当日の13時や16時に 0 に戻る
4. **デプロイを更新** … デプロイを管理 → 鉛筆 → 「新バージョン」→ デプロイ（URLは変わらない）。
   **保存しただけでは反映されません。ここまでやること**
5. **ブースを決める** … `部屋ID一覧.xlsx` を先生に渡し、ブースを出す部屋に○を付けてもらう →
   スプレッドシートの `ブース` シートに写す（B列の部屋IDを id 列へ）
6. **確認** … `?mode=admin` でログイン → どれかを「混雑」に → 来場者画面が20秒以内に赤くなる。
   画面いちばん下のバージョン表示が新しくなっていることも見る

これで動きます。あとは当日までに `index.html` の `CONFIG` を実際の内容に合わせるだけです。

| 直すところ | 中身 | 直さないとどうなるか |
|---|---|---|
| `CONFIG.HOURS` | `{ date:'2026-09-XX', open:'09:30', close:'15:00' }` | 開場前に開いた人へ「まだ開場前です」と出ない |
| `CONFIG.INFO.timetable` | ステージ等の進行 | 雛形のまま紙マップにも刷られる |
| `CONFIG.INFO.help` | 救護室・本部の**実際の場所** | 迷子・急病のときアプリが役に立たない |
| `CONFIG.ROUTES` の `denshi`/`all`/`food` | 実際の部屋ID（`1F-02` 形式） | コースのボタンを押しても何も起きない |
| `CONFIG.PACE_SEC` | 分類ごとの1人あたり秒数 | 待ち分数の見積もりがずれる |

詳細は以下。

### 開場・閉場の一括ボタンの注意

「開場：全ブースを空いています」は**全ブースを無条件に**「空いています・待ちなし」にします。
11:00開始のブースも「空いています」になるので、押したあとで
**遅れて始まるブースの担当者が「■ 休止中にする」に戻す**こと。
本部は開場直後に、遅れて始まるブースの一覧を読み上げて確認してください。

### 当日の朝にやること

1. 本部の端末で `?mode=admin` を開き、**当日チェックリスト**の「開場前」を上から潰す
2. `?mode=qr` を刷って切り、カードの帯の表示どおりに各ブース入口へ貼る（前日でも可）
3. `?mode=print` を刷って受付に置く（スマホを持たない来場者・電池切れ用）
4. 開場したら **「開場：全ブースを空いています」** を1回押す
5. 受付のPCで `?mode=board` を大画面に映す
6. 閉場したら **「閉場：全ブースを準備中」** を1回押す

---

## 0. 全体像

| 場所 | 役割 | 触るファイル |
|---|---|---|
| GitHub Pages | 来場者・係員が開く画面 | `index.html` `sw.js` `manifest.json` `og.png` |
| Google Apps Script | 混雑データの読み書きAPI | `Code.gs` |
| スプレッドシート | データの保管場所 | `ブース` `履歴` `お知らせ` シート |

パスワードはコードのどこにも書きません。GASのスクリプトプロパティ `ADMIN_PASS` だけが持ちます。

---

## 1. URL 一覧

| URL | 用途 | 誰が使うか |
|---|---|---|
| `.../bunnkasai/` | 校内マップ | 来場者 |
| `.../bunnkasai/?tab=list` | ブース一覧 | 来場者 |
| `.../bunnkasai/?tab=stamp` | スタンプラリー | 来場者 |
| `.../bunnkasai/?tab=info` | タイムテーブル・案内 | 来場者 |
| `.../bunnkasai/#map` `#list` `#stamp` `#info` | 同上（端末の戻るで行き来できる） | 来場者 |
| `.../bunnkasai/?booth=1F-02` | その部屋のブースを開く | QR・リンク用 |
| `.../bunnkasai/?booth=1F-02&qr=1` | 開いてスタンプ付与＋「いまここ」表示 | **ブース入口のQR** |
| `.../bunnkasai/?mode=admin` | 係員コンソール | 係員（要パスワード） |
| `.../bunnkasai/?mode=qr` | QR印刷シート | 準備担当 |
| `.../bunnkasai/?mode=print` | 配布用の紙マップ | 準備担当 |
| `.../bunnkasai/?mode=report` | 混雑の振り返り | 文化祭後の記録・報告 |
| `.../bunnkasai/?mode=layout` | 校舎配置エディタ | 地図を実際の配置に合わせるとき |
| `.../bunnkasai/?mode=board` | 本部用の大画面ボード | 受付・本部のPCで映しっぱなしにする |

`.../bunnkasai/` は `https://ma222222-source.github.io/bunnkasai/` の略。

---

## 2. スプレッドシートの構成

> **id 列には `部屋ID一覧.xlsx` の部屋IDを入れる。**
> 例：機械加工実習室でブースを出すなら id は `1F-02`。
> 図面のIDと一致した部屋が地図の上で色分けされる。
> 一致しないIDでも一覧・スタンプは動き、地図の下に「図面に未配置」として出るので取りこぼしにはならない。

> **Code.gs も差し替えが必要です（v18 と v22 で変更）。**
> 屋外(0)と3階を扱えるようにし、`validate()` が部屋IDの形と階を確かめるようにしました。
> さらに、係員画面からお知らせを出すための `notice` アクションを足しました。
> Apps Script に貼り直したあと、必ず「デプロイ → デプロイを管理 → 編集 → 新バージョン → デプロイ」まで行ってください。
> URLは変わりません。

### `ブース` シート

| A: id | B: name | C: status | D: time | E: category | F: floor | G: note | H: wait | I: image |
|---|---|---|---|---|---|---|---|---|
| 1 | 電子科 | 空いています | (自動) | 展示 | 1 | ロボット実演 11:00〜 | 0 | https://.../denshi.jpg |

- **status に入れてよい値は4つだけ**：`空いています` `やや混雑` `混雑しています` `準備中`
- `category` は自由。一覧画面のフィルタが自動で作られる
- `floor` は `1` か `2`。`2` にすると2Fマップに自動配置される
- `note` は一言メモ。「11:00 実演」など**時間の情報を入れると効果が大きい**
- `image` は写真のURL（任意）。一覧に小さく表示される。**何をやっているか分かるブースには人が来る**ので、
  1枚でも入れる価値がある。Googleドライブの共有リンクではなく直リンク（`.jpg` `.png` で終わるURL）を使うこと
- `time` と `wait` は係員の操作で自動更新される。手で触らない

### `お知らせ` シート

| A: message | B: level | C: enabled |
|---|---|---|
| 12:00から体育館でステージ発表です | info | TRUE |

- `C2` を `TRUE` にした瞬間から、全員の画面上部に表示される
- `level` は `info`（赤帯・通常）か `alert`（強調）
- 消すときは `C2` を `FALSE` に戻すだけ
- **緊急連絡にも使える。当日は本部がこのセルだけ管理すればよい**

### `来場者` シート

受付でカウントした人数が1行ずつ積まれる（`timestamp` / `delta` / `memo`）。
係員コンソールの「来場者カウンタ」から ＋1 / ＋5 / ＋10 で加算、−1 で取り消し。
当日ぶんの合計が来場者画面には出ず、**係員コンソールと大画面ボードにだけ**表示される。

### `履歴` シート

係員が更新するたびに1行増える。触らなくてよい。
このデータから来場者画面の「混雑の推移」グラフが作られる。1万行を超えると古い分から自動で削除される。

---

## 3. GAS のセットアップ

1. スプレッドシート → 拡張機能 → Apps Script
2. 既存コードを **全部消して** `Code.gs` の全文を貼り付け → 保存
3. 関数選択で **`migrate`** を選んで ▶実行
   - 初回は権限承認が出る → 許可
   - シートの作成と、足りない列（`wait` など）の追加を自動で行う
   - **何度実行しても安全**。既存データは消えない
4. 初回だけ **`seedBooths`** を実行するとブース8件の雛形が入る（既にデータがあれば何もしない）
5. パスワードを設定：左端の **歯車（プロジェクトの設定）** → 一番下の **スクリプト プロパティ** → `ADMIN_PASS` に係員用パスワードを入れて保存
   - **英数字12文字以上**にすること。4桁数字は総当たりで破られる
6. 前日に **`validate`** を実行すると、シートの不備（id重複・空欄・使えないステータス・パスワード未設定など）を実行ログに一覧してくれる。何も書き換えないので安心して実行できる
7. デプロイ → デプロイを管理 → 鉛筆アイコン → バージョン「新バージョン」→ デプロイ
   - こうすると **URLが変わらない**ので `index.html` を触らずに済む
   - 初回だけは「新しいデプロイ」→ ウェブアプリ（実行: 自分 / アクセス: 全員）

`migrate` を実行すると、シートの `status` 列がプルダウンになり、決められた4つ以外を
入力できなくなる。色分けも自動で付くので、シートを直接見ても状況が分かる。

### 動作確認

`/exec` URL をブラウザで開いて、次の形が返ればOK。

```json
{"ok":true,"updatedAt":"...","staleMinutes":45,"waitThresholds":{"warn":6,"busy":16},
 "notice":null,"booths":[{"id":"1","name":"電子科","status":"準備中","stale":false,"wait":0,...}]}
```

---

## 4. index.html の設定

ファイル冒頭の `CONFIG` だけ触ればよい。

```js
const CONFIG = {
  BUILD: '2026-08-05k',   // 更新したら日付を変える。画面下部に出るので反映確認に使える
  GAS_URL: '...',         // GASの /exec URL
  LIFF_ID: '',            // LIFF化するときだけ設定。空なら通常のWebとして動く
  PACE_SEC: {             // 待ち時間の目安を「分」で出すための、1人あたりの秒数
    '食べ物':35, '展示':25, '受付':20, '体験':90, 'イベント':0, _default:45
  },                      // 0 にするとそのカテゴリは分数を出さない
  ROUTES: { denshi:['4','1','7'], all:[...], food:['8','7','4'] },  // おすすめコースの巡回順
  INFO: {
    timetable: [ ['09:30','開場・受付開始'],
                 ['12:00','各科ステージ発表（体育館）','1F-42'] ],  // 3つめは場所（部屋ID・任意）
    help:      [ ['🏥','救護室','第1校舎1F 保健室'], ... ]
  }
};
```

### 校舎の図面について（重要）

`FLOOR_PLAN` は、学校の **校舎平面図（R8.4.1現在）の xlsx から自動で起こした実座標**。
セル結合が1部屋の長方形になっていたので、列・行の番号にスケールを掛けただけの実寸比。
**手で座標を打つ必要はない。**

| 階 | 内容 |
|---|---|
| 屋外（`0`） | 敷地図。正門・通用門・駐車場・駐輪場・グラウンド・記念館・弓道場など。校舎ブロックをタップすると1階へ入る |
| 1F | 各科実習棟・体育館・柔剣道場・北/南教室棟・本館1階など **91室** |
| 2F | 教室棟2階・本館2階・土木科/電子科実習棟・専攻科棟 **41室** |
| 3F | 教室棟3階・本館3階（美術室・音楽室・CAI教室・視聴覚室） **17室** |

全150室に `1F-01` のような **部屋ID** が振ってあり、同梱の **`部屋ID一覧.xlsx`** に
「階／部屋ID／部屋名」の対応表が入っている。ブースを出す部屋にそのIDを使う。

- **教室と実習室だけを載せている。** 準備室・職員室・事務室・書庫などは地図から外した。
  来場者にとって使えない情報を描いても、ごちゃつくだけなので
- ただし**外した部屋の座標は残してある**（`hidden`）。
  ブースを割り当てると、その部屋だけ自動で地図に現れる。
  保健室でも中会議室でも本館1階の部屋でも、id さえ書けば地図に出るので、
  「地図に無いから使えない」ということは起きない（`部屋ID一覧.xlsx` のE列を参照）
- **本館1階は「立入禁止」の1枚のブロック**として表示する。中の部屋は描かない
- **ブースが入っていない部屋も、うすく名前だけ描かれる。** 目印になるので消さないこと
- **廊下は青みのある帯**で塗ってあるので、通れる場所が一目で分かる
- 地図の下に**記号の凡例**（番号・訪問済み・まもなく・いまここ）がある。
  混雑の色だけでなく、地図に出る印を全部説明している
- **屋外マップには道路**（正門からの動線・8m道路・通用門まで）が入っている
- **科ごとに色分け**している。部屋の左端の色帯と、地図の下の凡例が対応する
  （色は `DEPTS` で変えられる。部屋と科の対応は `部屋ID一覧.xlsx` のD列）。
  色覚に多様性があっても見分けられるよう、P型・D型・T型の見え方を計算して選んである
  （いちばん似ている組でも 45/255 の差がある。変える場合は同じ確認をすること）
- **凡例の科をタップすると、その科だけ残る**。「電気科どこ？」に一発で答えられる
- **「🚻 トイレ・出入口」ボタン**でトイレと昇降口だけを目立たせられる。
  出入口（昇降口・玄関）は普段から緑で強調してある
- 初期表示では図面を切らない（端が隠れていると、そこに何かある事に気づけないため）。
  文字を読みたいときは ＋ボタン・ピンチ・「ブース」ボタンで拡大する
- 地図右上の **「ブース」ボタン**でその階のブースに寄り、**「全体」**で戻る
- 2本指のピンチ・ダブルタップでも拡大できる

位置を微調整したいときは `?mode=layout`。枠をドラッグで動かし、右下の角で大きさを変え、
「コードを出す」で `FLOOR_PLAN` にそのまま貼れる形が出る。
**書き出しは全項目を丸ごと写す**ので、貼り直しても科の色（`z`）や控えの部屋（`hidden`）は消えない。

- ドラッグ対象：部屋の枠・建物や屋外・設備（WC/階段）・見出し
- 2px単位で吸着する（「方眼」ボタンで切替）
- 部屋の追加・削除は `FLOOR_PLAN` を直接編集する（ブースの追加・削除はスプレッドシート側）

---

## 5. QRコードの印刷

各カードの上に**「1F　FA実習室　の入口へ」**のように貼る場所が入っています。
総合案内（黒い帯）だけURLを大きく刷ってあります。QRを読めない端末の人が打ち込めるようにするためです。
QRは自前で作っているので外部サービスに依存しません。生成した11枚すべてが実際に読み取れることを
`node qrshot.js && python3 qrdecode.py` で確認できます（URL 190文字まで対応、超えると
「QRを作れませんでした」と印字されます）。
刷ったら切って、その表示どおりに配って貼ってください。総合案内（黒い帯）は正門・受付用です。


1. `.../bunnkasai/?mode=qr` を **PCのChromeで開く**
2. 「印刷する」→ A4・**背景のグラフィックをON**
3. 1ページ6枚。ブース数+1枚（先頭は校門・ポスター用の総合案内）
4. 切って各ブースの入口・受付台に貼る

QRを読むと **そのブースの詳細が開き、スタンプが自動で入る**。
スタンプが自己申告でなくなるので、来場者の回遊が増える。

> QRコードは外部サービスを使わずこのファイル内で生成している。
> ネットが無い場所でも再印刷でき、CDN障害の影響も受けない。

最後のページは **係員用の手順カード**（ログイン用QR付き）。
掲示物ではないので、切り離して係員だけに配ること。

---

## 5-2. 配布用の紙マップ

`.../bunnkasai/?mode=print` → 「印刷する」→ **A4縦・背景のグラフィックON**

A4縦1枚に、1階/2階の地図・ブース一覧・タイムテーブル・困ったときの案内が収まる。
地図上の番号は画面のマップ・一覧と共通なので、紙とスマホを見比べられる。
右上のQRから最新の混雑状況に飛べる。

スマホを持たない来場者、電池切れ、通信が不安定な場合の保険になる。
内容はスプレッドシートと `CONFIG.INFO` から自動生成されるので、
ブース名やタイムテーブルを直せば紙マップにも反映される。

---

## 5-3. 混雑の振り返り（文化祭のあと）

ブース別の推移には**階と部屋名**も出る（「電子機械科 FA体験 / 1F・FA実習室」）。
来年見返したときに、どの部屋だったか分かるようにするため。画像で保存したPNGにも入る。


`.../bunnkasai/?mode=report`

`履歴` シートから、その日の混雑の移り変わりを自動で集計する。

- 記録された時間帯／最も混んだ時刻／最も混雑したブース／係員の更新回数
- 全体の混雑の移り変わり（15分ごとの積み上げグラフ）
- **来場者の入り**（受付でカウントしたぶんを15分ごとに集計）
- ブース別のヒートマップ（最大待ち人数つき）

印刷のほか、**「画像として保存（PNG）」で1枚の画像に書き出せる**（画面のスクリーンショットではなく、
報告書に貼る前提で描き直した専用の画像）。来年への引き継ぎ資料にそのまま貼れる。
複数日開催した場合は `?mode=report&date=2026-08-04` のように日付を指定できる
（省略すると記録の最終日）。

---

## 5-4. 本部用の大画面ボード

`.../bunnkasai/?mode=board` を受付や本部のノートPCで開いて、**そのまま映しっぱなしにする。**

- 全ブースを大きなタイルで表示。遠くからでも待ち人数が読める
- 更新が15分以上止まっているブースは赤い帯で名指しされる
  → 本部から「◯◯科、更新して」と声をかけられる。**更新忘れ対策の本命**
- 来場者数と現在時刻、最終同期も常時表示
- 対応端末では画面が消えないようにしている（Wake Lock）

プロジェクタや外部モニタに出す場合は、ブラウザを全画面（F11）にすること。

---

## 6. 係員の使い方（そのまま配布できる文面）

```
【混雑状況の更新方法】

1. 下のURLを開いてホーム画面に追加してください
   https://ma222222-source.github.io/bunnkasai/?mode=admin
2. パスワード：（当日配布）
3. 初回だけ「担当ブース」から自分の持ち場をタップ
   → 以後、自分のブースだけが表示されます

【更新のしかた】
・待ち人数を ＋1 / ＋5 で数えて「送信」を押すだけ
  → 人数から混雑度が自動で決まります
  （0〜5人=空き / 6〜15人=やや混雑 / 16人〜=混雑）
・数えられないときは下の3つのボタンでもOK
・受付担当は「来場者カウンタ」で入場者を ＋1／＋5／＋10 で数える

【お願い】
・15分に1回は更新してください
・30分を過ぎるとあなたのブースが赤く警告表示になります
・45分を過ぎると来場者側では「情報なし」になります
  （古い情報を見せないための仕組みです）
・画面が古いと感じたら、一番下の「表示を最新にする」をタップ
```

---

## 7. LINE公式アカウントとの連携

### リッチメニュー（コード不要・これだけでも十分）

LINE Official Account Manager → ホーム → リッチメニュー → 作成

| ボタン | アクション | 設定値 |
|---|---|---|
| 校内マップ | リンク | `.../bunnkasai/?tab=map` |
| いま空いてる所 | リンク | `.../bunnkasai/?tab=list` |
| スタンプラリー | リンク | `.../bunnkasai/?tab=stamp` |
| タイムテーブル | リンク | `.../bunnkasai/?tab=info` |

表示期間を当日に設定し、「デフォルト表示」をON。

### LIFF（LINEアプリ内で開く）

1. [LINE Developers](https://developers.line.biz/) でプロバイダー → LINEログインチャネル作成
2. LIFF タブ → 追加（サイズ **Full**、エンドポイントURL `.../bunnkasai/`）
3. `index.html` の `CONFIG.LIFF_ID` に発行されたIDを貼る
4. 発行された `https://liff.line.me/xxxxx` をリッチメニューのリンク先に差し替え

**時間がなければリッチメニューだけで問題ない。** ブラウザで開いても全機能が動く。

---

## 8. 当日までのチェックリスト

**前日まで**

- [ ] `ADMIN_PASS` を英数字12文字以上に設定した
- [ ] `ブース` シートの `name` を実際の出し物名にした
- [ ] `note` に実演・上演の時間を入れた
- [ ] `CONFIG.INFO.timetable` を実際の進行表にした
- [ ] `FLOOR_PLAN` の座標を実際の校舎配置に合わせた
- [ ] QRを印刷して、実機で読み取れることを確認した
- [ ] 配布用の紙マップを印刷して内容を確認した
- [ ] GASで `validate` を実行して問題が出ないことを確認した
- [ ] 係員全員がログインでき、担当ブースを設定済み
- [ ] 機内モードで開いて、直前の情報が表示されることを確認した
- [ ] iPhone / Android の両方で表示崩れがないことを確認した

**当日朝**

- [ ] 全ブースを「準備中」にしておく（GASで `resetDaily` を実行、または係員が個別に設定）
- [ ] `お知らせ` シートの `C2` が `FALSE` になっている
- [ ] 開場と同時に受付・本部を「空いています」に更新

**トラブル時**

| 症状 | 対処 |
|---|---|
| 画面が古いまま | 画面一番下の「表示を最新にする」をタップ。`BUILD` の日付で判別できる |
| 全ブースが「情報なし」 | GASが止まっている可能性。`/exec` を直接開いて確認 |
| 係員がログインできない | スクリプトプロパティ `ADMIN_PASS` を確認。デプロイが古い可能性も |
| 「しばらく受け付けできません」と出る | 誰かが10回連続で入力を間違えた状態。90秒待てば自動で解けます |
| 当日にパスワードを変えた | 係員が更新しようとした時点で自動的にログイン画面に戻る。新しいパスワードを伝えるだけでよい |
| 更新しても反映されない | GASは8秒キャッシュ、画面は20秒間隔。最大30秒待つ |
| ブースが中止・売り切れ・機材トラブルで止まった | 係員コンソールでそのブースの **「■ 休止中にする」** を押す。押さないと「空いています」のまま残り、来場者が無駄足になる。理由は本部が `note` 列に「【中止】雨天のため」と書き足す |
| ブースの場所が変わった | ①シートの `id` を移動先の部屋IDに書き換える ②**入口のQRを刷り直して貼り替える**（旧QRは「移動したかも」と出て一覧に飛ぶ）③旧場所にも貼り紙 ④「📣 お知らせを出す」で移動を周知 |
| 進行時間が変わった | タイムテーブルは当日変更できない。「📣 お知らせを出す」で『ステージは11:30に繰り上げます（表の表示は12:00のままです）』と、ずれを明記して出す |
| 係員がスマホを落とした／使えない | 隣の端末を借りて `?mode=admin` でログイン →「すべて表示」から該当ブースを更新する |
| 係員がログインし直しになった | パスワードは画面を閉じると消える。**当日はブラウザを閉じないこと**。本部はパスワードを書いた紙を1枚必ず持つ |
| 「何度も違っています」と出る | 同じ端末で連続して外した状態。1分ほど待てば解ける（他の係員には影響しない） |
| ファイルを差し替えたのに古い画面が出る | `sw.js` も一緒にアップロードしたか確認。回線が遅くて古い画面が先に出た場合は、数秒後に緑の「新しい案内が届いています／最新にする」バーが出るのでそれを押す。直らなければ画面下部の「表示が古いときはここをタップ」 |
| `manifest.json` が無い・名前が違う | ホーム画面追加とオフライン表示が効かなくなる。ルートに `manifest.json` があるか確認 |

---

## 8-2. 来場者向けの機能まとめ

| 機能 | 説明 |
|---|---|
| マップ | 混雑度を 色＋模様＋記号＋待ち人数 の4通りで表示。番号は紙マップと共通 |
| 地図 | **屋外／1F／2F／3F の4枚**。学校の平面図から起こした実際の部屋割り。ブースが入っていない部屋もうすく名前が出るので、目印になる |
| 地図の拡大 | 2本指のピンチ／ダブルタップ／右上の＋−ボタンで最大4倍まで。「ブース」ボタンでその階のブースに寄り、「全体」で戻る。階を変えると全体表示に戻る |
| 屋外マップ | 正門から入って最初に見る1枚。校舎ブロックをタップすると1階の地図に入る |
| ブース詳細シート | 地図の枠やおすすめをタップすると、下からシートが出て 混雑度・待ち人数・実演時間・推移 が見られる。画面は地図のまま動かないので迷わない。つまみを下へ引く／背景タップ／Esc／端末の戻る で閉じる |
| いまここ | ブース入口のQRを読むと、そこにいることを地図に表示（40分で自動消滅）。同じ階の空いているブースを1件おすすめする。位置情報の許可は求めない |
| いま行くならここ | 混雑度と未訪問を加味して上位3件を提示。「いまここ」が分かっているときは同じ階を優先 |
| まるごと一周コース | ブースが2つ以上あれば**自動で作られる**順路。屋外→1F→2F→3F の順に、各階では**近いブースから順にたどる**（貪欲法）。座標を並べ替えるだけだと校舎を何度も往復する順路になり、地図の線が交差して読めなくなる。`CONFIG.ROUTES` を組まなくても順路機能が使える |
| 空いている順コース | いま空いている順に回る順路。実演の時間が決まっているものだけ先に入る。混雑の分散に効く |
| 周回コース | `CONFIG.ROUTES` で定義した順に番号を振り、**地図に矢印で道すじを描く**（線は廊下側だけに引くのでブース名を隠さない）。地図の下に「このコースの回り方」として**次に行くところから6件だけ**並ぶ（「すべて見る」で全件）。階・いまの混み具合・到達数が出る。押すとそのブースの詳細が開く。階をまたぐコースはその旨を知らせる |
| 称号 | スタンプの進み具合で 🌱探検スタート → 🚶散策中 → 🎒黒工ツウ → 🔥あと少し → 👑黒工マスター と変わる。共有カードにも入る |
| 空いたら知らせる | 混んでいるブースを登録すると、空いた時点で画面に通知（アプリを開いている間のみ） |
| スタンプラリー | QRを読むと自動で加算。全制覇すると完走記録が出る |
| トイレ・救護室 | マップ上の点線の枠をタップするとインフォへ移動 |
| 表示を最新にする | 画面最下部。保存データを消して読み直す（「更新したのに古い」の自己解決用） |
| 文字の大きさ | インフォタブで「標準／大きく／**特大**」を切替。端末に保存される。特大は標準の約1.5倍。320pxの小さい端末でも横あふれ0px・地図の文字のはみ出し0件を確認済み |
| 検索 | ブース名だけでなく **部屋名（機械加工実習室など）と科** でも引ける |
| 混雑時の代わり | 混んでいるブースを開くと、いま空いている近くのブースを1つ提案する。同じ階を優先する。「混んでいます」で終わると引き返すしかないので |
| まもなく・開催中 | ブースの `note` に「11:00〜」のように時刻が書いてあると、20分前から「あと◯分」、開始後30分は「開催中」のバッジが自動で出る。一覧・地図・詳細・おすすめの全部に反映される。**係員が特別な入力をする必要はない**（いつもの書き方のままでよい） |
| タイムテーブルから地図へ | `CONFIG.INFO.timetable` の3つめに部屋IDを書いておくと、その行に「📍◯◯を地図で見る」が出る。押すとその階に切り替わって場所が光る |
| スタンプから詳細へ | スタンプ画面の枠を押すと、そのブースの詳細が開く |
| 効果音 | インフォタブで切替。**初期値はオフ**（校内で勝手に音が鳴らないように）。スタンプ獲得と全制覇のときだけ短く鳴る |
| 待ち時間の目安 | 「24人待ち（およそ10分待ち）」のように分数も出す。`CONFIG.PACE_SEC` のカテゴリ別「1人あたりの秒数」×人数で計算する粗い見積もり。**当日、実際の回転を見て調整すること**（多めに見積もった方が苦情が出ない）。5分を超えたら5分刻みに丸める |
| スタンプカードの共有 | 回った記録を1枚の画像にして共有できる。スマホは共有シート、PCは画像として保存。画像は端末の中だけで作るのでサーバーには何も送らない |
| 戻るボタン | Androidの戻るで前のタブに戻る（アプリごと閉じない） |

## 8-3. 係員向けの機能まとめ

| 機能 | 説明 |
|---|---|
| 担当ブース | 選んだブースだけを表示。誤操作を防ぐ。チップには**階と部屋名も出る**ので、名前が似ていても取り違えない。選択は端末に保存される |
| 待ち人数 | ＋1/＋5で数えて送信。人数から混雑度が自動で決まる |
| 3段階ボタン | 数えられないとき用。押すと待ち人数は消える（矛盾表示を防ぐため） |
| 元に戻す | 更新直後に出るボタンで、押し間違いを1タップで戻せる |
| 経過時間 | 更新が古い順に並ぶ。15分で注意、30分で警告 |
| 入力の保護 | 20秒ごとの自動同期で、入力途中の数字や並び順が変わらないようにしてある |
| ボードの密度 | ブースが13件を超えるとタイルが自動で詰まる（13〜24で中、25以上で小）。40件でも1画面に収まることを確認済み。壁掛けの画面はスクロールできないので、全部見えることを優先している |
| 本部ボードの地図 | タイルの右に各階の地図が並び、番号と混雑の色で「どの棟が混んでいるか」が一目で分かる。係員をどこに回すか即断するため。大画面に映しっぱなしにする前提なので操作は付けていない |
| 来場者カウンタ | 受付担当が入場者数を記録。当日合計はボードにも出る |
| 場所の表示 | 係員カード・本部ボードの両方に「2F・図書室」のように階と部屋名が出る。担当の取り違えを防ぐ |
| お知らせを出す | 係員画面から、来場者全員の画面上部に出る帯をその場で書ける。「お知らせ（青）」と「注意（赤）」の2種類。落とし物・ステージ開始・雨天対応など、当日は**すぐ出せること**に価値がある。スプレッドシートを開く必要はない |
| 開場・閉場の一括 | 係員画面の下。「開場：全ブースを空いています」「閉場：全ブースを準備中」の2つ。閉場を1タップにしてある（片付けの時間に全ブース分の操作を残すと、そのまま放置される） |
| 階を覚える | 前回見ていた階を次に開いたときも表示する。初回はブースがいちばん多い階から始まる |
| 入力ミスの警告 | 係員画面の上に出る。**id の重複**（後の行を無視した旨）と、**図面に無い id**（一覧には出るが地図に載らない旨）を名指しで知らせる |
| 当日チェックリスト | 係員画面の上部。開場前6項目／開場後3項目／閉場後4項目。**その日限り**の記録（日付が変わると自動でリセット）。端末ごとに保存されるので、本部の1台で使うこと |

---

## 9. 仕組みの要点（引き継ぎ用）

- **負荷対策**：GETは8秒キャッシュ。来場者が何人開いてもGASの実行回数は増えない
- **競合対策**：書き込みは `LockService` で直列化。複数の係員が同時に押しても壊れない
- **古い情報を出さない**：45分更新がないブースはサーバー側で「情報なし」に落とす
- **オフライン・遅い回線**：Service Workerで画面の枠をキャッシュ。ネットワークを2.5秒待って
  遅ければ保存分を先に表示する（校内Wi-Fiが詰まっても画面が出る）。
  混雑データはキャッシュしない（古い混雑状況を見せないため）
- **色に頼らない**：混雑度は色＋模様＋記号＋人数の4通りで表現。色覚特性や直射日光下でも判別できる
- **絵文字を使わない**：画面の記号はすべて自前のSVG。端末ごとに絵柄が変わらない
- **横向き対応**：縦が540px以下のときは情報を畳み、地図を先頭に出す
- **総当たり対策**：連続10回失敗で90秒ロック（サーバー側）
- **動き**：状況が変わったブースは一瞬だけ光る、スタンプは獲得時に弾む、数字は数え上がる。
  いずれも `prefers-reduced-motion`（動きを減らす設定）を有効にした端末では自動的に止まる
- **外部依存ゼロ**：CDNもライブラリも使っていない。QRコードも自前生成
- **検証**：次の項目を自動テストで確認済み
  - QRコードは生成結果を実際にデコードして全バージョン（1〜10）で照合
  - GASのロジック単体、およびフロントとの結合（実物のCode.gsを動かしたE2E）
  - 画面幅 320 / 390 / 768px で横あふれなし、20ブース・長い名前でも崩れない
  - アクセシビリティ（WCAG 2.1 AA）を全画面×両テーマで違反ゼロ
  - 色覚特性（1型・2型・3型）のシミュレーションで、色以外の手がかりで判別できること
  - 印刷結果をPDF化して、紙マップA4縦1枚・レポートA4縦1枚に収まること
- **実測値**（gzip配信、ブース12件、画面幅390px）
  - 通常回線：地図が出るまで 約0.3秒
  - 低速3G相当（400kbps・遅延400ms）：約1.4秒
  - 20秒ごとの再描画：1回あたり 約1.5ms
  - HTML転送量：約41KB（外部リクエストは0件）
